@model Order
@{
    ViewData["Title"] = "Підтвердження замовлення";
}

@section Styles {
    <link rel="stylesheet" href="~/css/order.css" asp-append-version="true" />
}

<div class="container">
    <div class="confirmation-container fade-up">
    <div class="confirmation-icon scale-in-bounce">
        <svg xmlns="http://www.w3.org/2000/svg" width="80" height="80" viewBox="0 0 24 24" fill="none" stroke="#28a745" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"></path>
            <polyline points="22 4 12 14.01 9 11.01"></polyline>
        </svg>
    </div>
    
    <h1 class="confirmation-title">Замовлення @(Model.OrderStatus == "Paid" ? "успішно оплачено!" : "успішно оформлено!")</h1>
    <p class="confirmation-message">Дякуємо за ваше замовлення. Ми вже почали готувати! 👨‍🍳</p>

    <div class="order-details-card">
        <h3 class="card-title">Деталі замовлення</h3>
        <p><strong>Номер замовлення:</strong> #@Model.Id</p>
        <p><strong>Ім'я:</strong> @Model.CustomerName</p>
        <p><strong>Телефон:</strong> @Model.CustomerPhone</p>
        <p><strong>Адреса:</strong> @Model.CustomerAddress</p>
        <p><strong>Сума:</strong> @Model.TotalAmount.ToString("c")</p>
        <p>
            <strong>Статус:</strong> 
            @if (Model.OrderStatus == "Paid")
            {
                <span class="badge bg-success status-badge">Оплачено ✅</span>
            }
            else
            {
                <span class="badge bg-warning text-dark status-badge">Очікується (Готівка) 🕒</span>
            }
        </p>
    </div>

    <a asp-controller="Home" asp-action="Index" class="btn btn-primary mt-3">Повернутися на головну</a>
</div>        </div>
</div>
